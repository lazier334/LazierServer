name: Build Docker Image

on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Docker tag (format: vX.Y.Z)'
        required: true
        type: string
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write  # 推送到容器注册表需要这个权限
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
    
    - name: Get tag info
      id: tag
      run: |
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          TAG_NAME="${{ github.event.inputs.tag_name }}"
        else
          TAG_NAME=${GITHUB_REF#refs/tags/}
        fi
        REPO_NAME=$(echo "${{ github.event.repository.name }}" | tr '[:upper:]' '[:lower:]')
        echo "TAG_NAME=$TAG_NAME" >> $GITHUB_ENV
        echo "VERSION=${TAG_NAME#v}" >> $GITHUB_ENV
        
        # 设置Docker镜像标签
        echo "DOCKER_IMAGE=ghcr.io/${{ github.repository_owner }}/$REPO_NAME" >> $GITHUB_ENV
        echo "DOCKER_TAG=$TAG_NAME" >> $GITHUB_ENV
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
      with:
        driver: docker-container
        driver-opts: |
          image=moby/buildkit:master
    
    - name: Log in to GitHub Container Registry
      if: ${{ github.event_name != 'workflow_dispatch' || github.ref_type != 'tag' }}
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        file: ./Dockerfile
        platforms: linux/amd64,linux/arm64
        push: ${{ github.event_name != 'workflow_dispatch' }}
        tags: |
          ${{ env.DOCKER_IMAGE }}:${{ env.DOCKER_TAG }}
          ${{ env.DOCKER_IMAGE }}:latest
        labels: |
          org.opencontainers.image.version=${{ env.VERSION }}
          org.opencontainers.image.created=$(date -u +'%Y-%m-%dT%H:%M:%SZ')
          org.opencontainers.image.revision=${{ github.sha }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
    
    - name: Update image description
      if: ${{ github.event_name != 'workflow_dispatch' }}
      run: |
        # 更新容器镜像描述
        curl -X PATCH \
          -H "Accept: application/vnd.github.v3+json" \
          -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
          https://api.github.com/user/packages/container/${{ github.event.repository.name }}/versions \
          -d "{\"metadata\":{\"description\":\"${{ github.event.repository.name }} version ${{ env.VERSION }}\"}}"
