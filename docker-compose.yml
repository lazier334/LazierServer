services:
  app:
    build: .
    container_name: LazierServer
    restart: unless-stopped
    ports:
      - "3000:3000"
      - "3001:3001"
      - "3010:3010"
      - "3011:3011"
    environment:
      - NODE_ENV=production
      - TZ=Asia/Shanghai
    volumes:
      - ./ld/:/app/ld/